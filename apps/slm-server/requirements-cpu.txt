# ═══════════════════════════════════════════════════════════════════════════
# apps/slm-server/requirements-cpu.txt
# CPU PROFILE — includes torch/vllm/transformers explicitly, since (unlike
# the gpu profile's vllm/vllm-openai base image) there is no base image
# providing these for a plain python:3.11-slim CPU container.
# ═══════════════════════════════════════════════════════════════════════════

# 1. API GATEWAY & STREAMING CORE
fastapi==0.136.3
uvicorn[standard]==0.52.2
pydantic==2.13.4
sse-starlette==3.4.8
httpx[http2]==0.28.1
h2>=4.1.0
brotli>=1.1.0
zstandard>=0.23.0
python-dotenv>=1.0.1
slowapi>=0.1.9
huggingface_hub>=0.26.0
prometheus_fastapi_instrumentator>=7.0.0

# 2. ZERO-HOP RAG ENGINE (CPU/Memory Subsystem) — identical across profiles
numpy==2.3.5
safetensors==0.8.0
rank-bm25==0.2.2
jsonschema==4.26.0
aiosqlite>=0.20.0
beautifulsoup4>=4.12.3
lxml>=5.3.0
redis==5.2.1
sentence-transformers==5.7.0
accelerate==1.10.0
peft==0.18.1

# 3. CPU INFERENCE STACK
# torch CPU build (no CUDA runtime bundled — much smaller download than the
# GPU wheel, and required since this profile has no GPU base image to
# inherit torch from).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

# vLLM's CPU backend. Verify this exact version publishes a CPU wheel (or
# build from source with VLLM_TARGET_DEVICE=cpu, as set in Dockerfile.cpu,
# against the torch build above) before deploying — CPU wheel availability
# varies by release; pin whatever version you've verified works on your
# target CPU (AVX512-BF16/AMX support required for bfloat16 throughput).
vllm==0.27.1
transformers==5.15.0
